@model List<SchedulingMeetings.Web.Models.Room>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1 class="text-center" style="font-family:'Gill Sans', 'Gill Sans MT', Calibri, 'Trebuchet MS', sans-serif">
    Lista de Salas
</h1>
<br />

<a asp-controller="room" asp-action="create" class="btn btn-primary btn-lg pull-right"><i class="fas fa-plus-circle"></i> Novo</a>
<br />
<br />

<table class="table table-hover">
    <thead>
        <tr>
            <th scope="col">Nome da Sala</th>
        </tr>
    </thead>
    @*@foreach (var item in Model)
    {

        <tbody>
            <tr>
                <td>@item.RoomName</td>
            </tr>
        </tbody>
    }*@
</table>
